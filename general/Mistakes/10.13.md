10.13

**Strobogrammatic Number ii**

	after get result from recursively call, we are going to build a new list to return

```
	for(int i = 0; i < list.size(); i ++){
		
		list.append(....) // -> cause infinit loop, size will dynamicly change
	}
```
	and we do not include length = n -2's result into the final result, so we create a new list



**StringComparator**


	```return c1 - c2```

	normal order -> o1 smaller -> have higher priority


	String.length**()**  is a METHOD	!!!!



**Add number**

	get numeric value from string, use 

	```char - '0'```

